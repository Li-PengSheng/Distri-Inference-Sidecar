[package]
name = "rust_ops"
version = "0.1.0"
edition = "2024"

[lib]
# Compile as a C-compatible static library so it can be linked into Go (via
# CGo) or any other C-compatible consumer.
crate-type = ["staticlib"]

[dependencies]
